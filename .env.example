# Turso cloud sync + auth (required for accounts)
# Copy this file to .env and fill in your values.
# The VITE_ prefix is mandatory — Vite only exposes prefixed vars to the browser.
VITE_TURSO_DATABASE_URL=
VITE_TURSO_AUTH_TOKEN=
